apiVersion: v1
kind: Service
metadata:
  name: sales-service
  namespace: eventticketing
  labels:
    app: sales-service
spec:
  selector:
    app: sales-service
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  type: ClusterIP