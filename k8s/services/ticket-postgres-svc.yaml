apiVersion: v1
kind: Service
metadata:
  name: ticket-postgres
  namespace: eventticketing
spec:
  clusterIP: None
  selector:
    app: ticket-postgres
  ports:
    - port: 5432
      targetPort: 5432
