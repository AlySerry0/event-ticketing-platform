apiVersion: v1
kind: Service
metadata:
  name: ticket-service
  namespace: eventticketing
  labels:
    app: ticket-service
spec:
  selector:
    app: ticket-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
