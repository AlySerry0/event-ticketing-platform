apiVersion: v1
kind: Service
metadata:
  name: booking-postgres
  namespace: eventticketing
spec:
  selector:
    app: booking-postgres
  ports:
    - port: 5432
      targetPort: 5432
  clusterIP: None
